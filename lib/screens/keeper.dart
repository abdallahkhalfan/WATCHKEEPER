import 'package:cloud_firestore/cloud_firestore.dart';import 'package:flutter/material.dart';import '../services/services.dart';
class KeeperPage extends StatefulWidget{const KeeperPage({super.key});@override State<KeeperPage> createState()=>_S();}
class _S extends State<KeeperPage>{final t=TextEditingController();Future<void> send()async{final q=t.text.trim();if(q.isEmpty)return;t.clear();await DB.keeper.add({'role':'user','text':q,'at':FieldValue.serverTimestamp()});final l=q.toLowerCase();String a='I’ve got it. You can also save it in Planner or Memories so I can keep it under watch.';if(l.contains('today'))a='Open Planner to see everything scheduled for today. I’ll keep your notifications ready.';if(l.contains('remember'))a='That sounds worth keeping. Use Memories to attach the photo, video or story and save it permanently.';await DB.keeper.add({'role':'keeper','text':a,'at':FieldValue.serverTimestamp()});}@override Widget build(BuildContext c)=>Column(children:[Padding(padding:const EdgeInsets.fromLTRB(18,18,18,8),child:Row(children:[Container(width:46,height:46,decoration:const BoxDecoration(shape:BoxShape.circle,gradient:LinearGradient(colors:[Color(0xFF7C5CFF),Color(0xFF38D6FF)])),child:const Icon(Icons.auto_awesome)),const SizedBox(width:12),const Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text('Keeper',style:TextStyle(fontSize:24,fontWeight:FontWeight.w900)),Text('Your personal watchkeeper',style:TextStyle(color:Colors.white60))])])),Expanded(child:StreamBuilder<QuerySnapshot<Map<String,dynamic>>>(stream:DB.keeper.orderBy('at',descending:true).limit(80).snapshots(),builder:(c,s){final d=s.data?.docs??[];if(d.isEmpty)return const Center(child:Padding(padding:EdgeInsets.all(35),child:Text('Talk to Keeper.\n“Remember this…”  •  “What do I have today?”',textAlign:TextAlign.center,style:TextStyle(fontSize:18,color:Colors.white60))));return ListView.builder(reverse:true,padding:const EdgeInsets.all(16),itemCount:d.length,itemBuilder:(c,i){final x=d[i].data(),me=x['role']=='user';return Align(alignment:me?Alignment.centerRight:Alignment.centerLeft,child:Container(margin:const EdgeInsets.symmetric(vertical:5),padding:const EdgeInsets.symmetric(horizontal:16,vertical:12),constraints:BoxConstraints(maxWidth:MediaQuery.sizeOf(c).width*.78),decoration:BoxDecoration(gradient:me?const LinearGradient(colors:[Color(0xFF6D4AFF),Color(0xFF8C5CFF)]):null,color:me?null:Colors.white.withValues(alpha:.09),borderRadius:BorderRadius.only(topLeft:const Radius.circular(22),topRight:const Radius.circular(22),bottomLeft:Radius.circular(me?22:5),bottomRight:Radius.circular(me?5:22))),child:Text(x['text']??'')));});})),SafeArea(top:false,child:Padding(padding:const EdgeInsets.fromLTRB(12,8,12,12),child:Row(children:[IconButton(onPressed:()=>ScaffoldMessenger.of(c).showSnackBar(const SnackBar(content:Text('Use Memories to attach photo/video'))),icon:const Icon(Icons.add_circle_outline)),Expanded(child:TextField(controller:t,onSubmitted:(_)=>send(),decoration:const InputDecoration(hintText:'Ask WATCHKEEPER anything…'))),const SizedBox(width:8),FloatingActionButton.small(onPressed:send,child:const Icon(Icons.arrow_upward_rounded))]))) ]);}
