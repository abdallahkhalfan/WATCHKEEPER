name: watchkeeper
description: WATCHKEEPER 5 Premium
publish_to: "none"
version: 5.0.0+5

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  firebase_storage: ^13.0.1

  shared_preferences: ^2.5.3
  geolocator: ^14.0.2
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
  flutter_timezone: ^5.0.1
  image_picker: ^1.2.0
  url_launcher: ^6.3.2
  intl: ^0.20.2
  share_plus: ^11.1.0

flutter:
  uses-material-design: true