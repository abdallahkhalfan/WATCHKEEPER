name: watchkeeper
description: WATCHKEEPER 4
publish_to: "none"
version: 4.0.0+4

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  geolocator: ^14.0.2
  flutter_local_notifications: ^22.0.1
  timezone: ^0.11.1
  flutter_timezone: ^5.0.1
  image_picker: ^1.2.0
  url_launcher: ^6.3.2
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  cloud_firestore: ^6.0.1
  intl: ^0.20.2

flutter:
  uses-material-design: true
