name: watchkeeper
description: WATCHKEEPER 3 - personal departure, task, event and memory companion
publish_to: "none"
version: 3.0.0+3

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  image_picker: ^1.2.3

flutter:
  uses-material-design: true
