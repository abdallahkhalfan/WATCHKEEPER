name: watchkeeper
description: Smart departure, event preparation and safety reminder app.
publish_to: "none"
version: 2.0.0+2

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.3
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0

flutter:
  uses-material-design: true
