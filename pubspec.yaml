name: watchkeeper
description: WATCHKEEPER 4 - smart reminder, departure guard, planner, buddy and memories companion
publish_to: "none"
version: 4.0.0+5

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.3.0
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  geolocator: ^14.0.3
  image_picker: ^1.2.3
  url_launcher: ^6.3.2

flutter:
  uses-material-design: true
