name: watchkeeper
description: Smart departure and event preparation reminder
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.3.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0
flutter:
  uses-material-design: true
